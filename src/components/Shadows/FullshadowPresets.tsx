import React from "react";
import { SvgXml } from "react-native-svg";

export const FullshadowPresets = {
    button: {
        offset: [0, 0], distance: 10, startColor: 'rgba(101, 111, 153, .2)'
    } as ShadowProps,
};