import React from "react";
import { SvgXml } from "react-native-svg";
import {Shadow} from 'react-native-shadow-2';

export const ButtonshadowPresets = {
    button: {
        offset: [0, 0], distance: 4, startColor: 'rgba(101, 111, 153, .1)', radius: 50,
    } as ShadowProps,
  };